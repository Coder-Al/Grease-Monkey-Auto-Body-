/**
 * main.js - Main entry point that initializes all modules
 * 
 * This file ensures all scripts load in the correct order.
 * Since we're using separate script tags in HTML, this is optional.
 * But it's useful if you want to bundle everything later.
 */

// This file can be empty or just contain initialization logic
// For now, the individual modules (menu.js, form.js) initialize themselves
// using DOMContentLoaded, so main.js isn't strictly needed.

// However, if you want a single point of control:
console.log('Site initialized - Precision Auto Body');